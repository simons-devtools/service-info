import React from 'react';
import './OrderCrossing.modules.css';

const OrderCrossing = ({ modalTwoApp }) => {
    console.log(modalTwoApp);

    return (
        <main>
            <h2>Order crossing page</h2>
        </main>
    );
};

export default OrderCrossing;