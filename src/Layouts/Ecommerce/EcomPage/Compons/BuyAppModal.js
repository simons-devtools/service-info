import React from 'react';
import './BuyAppModal.modules.css';

const BuyAppModal = ({ modalTwoApp }) => {
    // console.log(modalTwoApp);

    return (
        <div>
            <h2>Buy {modalTwoApp.name} modal compons</h2>
        </div>
    );
};

export default BuyAppModal;